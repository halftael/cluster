# 因为没有找到sgd网站中已分类好的gene数据 所以决定根据已有go注释数据生成有向无环图
# 一般是以富集数据集生成有向无环图，全基因组生成的图/类树 可以预见的树高值极大
# 因此 需要需要记录树的最高层数，根据具体数据因地制宜的改变要生成树的树高
# 定义一个node数据类型：父节点 - gene id- 基因数量 - 层数 - 子节点，为此需要准备两个键值对 {节点：gene} {父节点：子节点集}
# 根据网上已有的树结构生成代码 尝试生成node数据类型生成树，最坏结果是根据已有结果 根据层数添加括号嵌套。

# 实际上是失败了，由于生成的树大多数的节点都是一级节点，所以生成的树很‘扁’，几乎不具有建树的价值
# 改变思路 直接以节点为单位，求最佳节点集